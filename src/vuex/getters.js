const getters = {
    corpid: state => state.common.corpid,   // 企业cropid
    corpsecret: state => state.common.corpsecret,   // 应用
    access_token: state => state.common.access_token,   // 企业cropid
    isLoading: state => state.common.isLoading,   // 企业cropid
    alertShow: state => state.common.alertShow,   // 企业cropid
}
export default getters